name = "pickles-website"
main = "index.js"
compatibility_date = "2024-05-13"

[site]
bucket = "."

[build]
command = ""

# For Cloudflare Pages, we don't need routes in wrangler.toml
# Routes are handled by the _redirects file and Pages configuration
